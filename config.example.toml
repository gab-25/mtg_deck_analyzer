# MTG Deck Analyzer configuration.
# Copy this file to "config.toml" and fill in your values.
# CLI arguments always take precedence over these settings.

# Google Gemini API key, used for card translation and deck analysis.
api_key = "your_gemini_api_key_here"

# Default target language code for card names and details.
# Examples: "en", "it", "es", "fr", "de".
lang = "it"
